machine: 
  services: 
    - docker
 
dependencies: 
  override: 
    - npm install
    - npm install grunt-protractor-webdriver
    - npm install grunt-protractor-runner
    - bower install

deployment: 
  hub: 
    branch: feature_protractor
    commands: 
      - docker login  -u dqmaster -e dqmaster@gmail.com -p abc123 tutum.co
      - docker info
      - docker build -t tutum.co/dqmaster/yeoman3 .
      - docker push tutum.co/dqmaster/yeoman3